"""Coupon Service - MVP Stub.

This module will implement coupon CRUD, validation (expiry, usage limit,
min booking amount), discount calculation (percentage/fixed with cap),
and usage tracking.
TODO: Implement in next iteration.

Requirements: 17.1, 17.2, 17.3, 17.4, 17.5
See design.md for full specification.
"""

# TODO: Implement coupon service
# - create_coupon(code, discount_type, discount_value, ...): validate unique code
# - validate_coupon(code, booking_amount): check expiry, usage, min amount
# - calculate_discount(coupon, booking_amount): apply cap
# - apply_to_booking(booking_id, code): validate + increment usage + update booking
# Requirements: 17.1, 17.2, 17.3, 17.4, 17.5
