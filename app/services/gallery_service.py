"""Gallery Service - MVP Stub.

This module will implement image upload validation (JPEG/PNG/WebP, max 10MB),
metadata storage, display order management, and deletion.
TODO: Implement in next iteration.

Requirements: 14.1, 14.2, 14.3, 14.4
See design.md for full specification.
"""

# TODO: Implement gallery service
# - upload_image(entity_type, entity_id, file): validate MIME and size, store metadata
# - list_for_entity(entity_type, entity_id): sorted by display_order
# - delete_image(image_id): remove metadata, mark file for cleanup
# - update_display_order(image_id, new_order)
# Requirements: 14.1, 14.2, 14.3, 14.4
