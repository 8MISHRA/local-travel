"""Notification Service - MVP Stub.

This module will implement notification creation, listing (paginated,
sorted by created_at desc), mark_read, and mark_all_read.
TODO: Implement in next iteration.

Requirements: 19.1, 19.2, 19.3, 19.4
See design.md for full specification.
"""

# TODO: Implement notification service
# - create_notification(user_id, type, title, body, reference_type, reference_id, channel)
# - list_for_user(user_id, page, per_page): sorted by created_at desc
# - mark_read(notification_id): set is_read=True, record read_at
# - mark_all_read(user_id)
# Requirements: 19.1, 19.2, 19.3, 19.4
