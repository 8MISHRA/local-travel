"""Support Ticket Service - MVP Stub.

This module will implement support ticket creation, reply management,
status transitions (open -> in_progress -> resolved -> closed),
and customer-scoped listing.
TODO: Implement in next iteration.

Requirements: 20.1, 20.2, 20.3, 20.4
See design.md for full specification.
"""

# TODO: Implement support service
# - create_ticket(customer_id, subject, description, priority, booking_id)
# - add_reply(ticket_id, author_id, body): store reply, trigger notification
# - update_status(ticket_id, new_status)
# - list_for_customer(customer_id, page, per_page, status_filter)
# Requirements: 20.1, 20.2, 20.3, 20.4
